Você disse:
biblioteca string em c
#include <string.h>

Você disse:
sintaxe string
#include <stdio.h>
#include <string.h>

int main() {
    char nome1[50] = "Chat";
    char nome2[] = "GPT";

    strcat(nome1, nome2); // Concatena "GPT" em "Chat"
    printf("Resultado: %s\n", nome1);

    printf("Tamanho: %zu\n", strlen(nome1));

    if (strcmp(nome1, "ChatGPT") == 0) {
        printf("As strings são iguais!\n");
    }

    return 0;
}

Você disse:
conversao graus celsius para Fahrenheit
C=(°F−32)×95/5